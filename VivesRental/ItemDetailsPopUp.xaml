<Window x:Class="VivesRental.ItemDetailsPopUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VivesRental"
        mc:Ignorable="d"
        Title="ItemDetailsPopUp" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Name="ItemBox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Width="100" Header="Name"/>
                <DataGridTextColumn Binding="{Binding Description}" Width="100" Header="Description"/>
                <DataGridTextColumn Binding="{Binding Manufacturer}" Width="100" Header="Manufacturer"/>
                <DataGridTextColumn Binding="{Binding Publisher}" Width="100" Header="Publisher"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
